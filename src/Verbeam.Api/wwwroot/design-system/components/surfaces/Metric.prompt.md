Runtime read-out used in the inspector's runtime grid (provider / model / ocr / asr / cache).

```jsx
<Metric icon="ri-cpu-line" label="model" value="qwen2.5:7b" />
```

Requires RemixIcon loaded (the system's icon font). Icon + mono label + strong value, on a faint raised row.
