Sidebar nav item — the workspace list (Translate / OCR / Audio / …) and the settings section list.

```jsx
<NavButton active>Translate</NavButton>
<NavButton>OCR</NavButton>
```

Left-aligned, full width, ghost until `active` (filled `#2a2a2a`). Title Case labels in the workspace nav; Title Case section names in settings.
