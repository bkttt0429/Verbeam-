Compact mode switcher in the composer footer. Labels are lowercase.

```jsx
<Tab active>text</Tab>
<Tab>ocr</Tab>
<Tab>audio pipe</Tab>
```

Same ghost→filled treatment as NavButton but centered, smaller text, lowercase. Use for the in-composer mode row; use NavButton for the sidebar.
