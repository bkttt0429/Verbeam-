Label/value row for inspector panels and settings status stacks (available / source / engine / blocks counts).

```jsx
<StatusLine label="available" value="yes" valueColor="var(--success)" />
<StatusLine label="engine" value="windows-ocr" />
```

Muted mono label on the left, strong value on the right. Use `valueColor` to color affirmative/negative values.
