Amber inline hint for OCR/ASR engine guidance and provider notes.

```jsx
<Notice>預設本機 OCR，輕量，適合一般文字</Notice>
```

One soft-amber tone only — reserve for advisory text, not errors (errors live in the terminal as a `Message kind="error"`).
