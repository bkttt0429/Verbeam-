Wraps any control with the workbench's lowercase mono label (and optional hint). Use for every labelled form row.

```jsx
<Field label="source" htmlFor="src">
  <Input id="src" value="ja" />
</Field>
```

Labels are always lowercase mono grey (`--text-label`). Pass `hint` for the muted help line seen under file drop zones.
